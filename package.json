{
  "name": "css-layout-masterclass",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "gh-pages": "^6.2.0",
    "prettier": "^3.3.3"
  },
  "homepage": "https://Shinji0133.github.io/css-layout-masterclass",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
